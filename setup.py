from setuptools import setup

setup(
    name = "pyprimary",
    version = "0.1",
    packages = ["pyprimary"],
    install_requires = ["bson"],
    
    author = "PinePunk16",
    url = "https://github.com/PinePunk16/PyPrimary",
    description = "A python library for an easy displaying, generating, saving and loading of objects with unique IDs.",
)